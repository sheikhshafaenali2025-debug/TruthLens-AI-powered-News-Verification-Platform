services:
  - type: web
    name: truthlens-backend
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.4
      - key: CORS_ORIGINS
        value: "*"   # tighten to your Netlify URL after first deploy, e.g. https://your-site.netlify.app
      - key: FLASK_DEBUG
        value: "false"
